schema: spec-driven

context: |
  本项目为前后端合体（fullstack）项目，openspec/specs/ 下含 frontend/ 和 backend/ 子目录。
  AI 生成工件时须区分前后端影响范围。完整上下文见 openspec/specs/project.md。

# 快速通道判定标准（满足以下全部条件时可用 /openspec-quick 跳过 design gate）
quick_change_criteria: |
  以下情形均可走快速通道（缺一不可）：
  1. 改动范围 ≤ 3 个文件
  2. 不涉及新功能、架构调整、安全敏感逻辑
  3. Root cause 明确（bug 修复）或改动意图无歧义（文案/样式）
  适用类型：纯文案/翻译调整、CSS 样式修改（不改组件结构）、
            已知 bug 修复、文档更新。
  不适用：任何涉及数据流变更、新接口、外部调用、配置处理的改动。

rules:
  proposal:
    - 提案控制在 600 字以内。
    - 必须包含「非目标」章节，明确圈定边界。
    - 安全规则：禁止在任何工件中写入 Token、密码、凭证或内部地址。
      只引用配置键名，不写实际值。

  specs:
    - Delta spec 须用「扩展/覆盖」语言引用 system.md 中已有基线，不重复陈述。

  design:
    - |
      关卡：在撰写 design.md 前，必须调用 /plan-eng-review 和 /plan-design-review。
      前者审查工程设计，后者审查 UI/交互设计。
      将两个关卡结果均记录在 design.md 最顶部：

        ## 关卡：工程审查
        状态：通过 | 仅警告 | 阻断
        审查者：gstack/plan-eng-review
        问题清单：<逐条列出，或填「无」>

        ## 关卡：UI 设计审查
        状态：通过 | 仅警告 | 阻断
        审查者：gstack/plan-design-review
        问题清单：<逐条列出，或填「无」>

      任意关卡状态为「阻断」则立即停止，不得继续撰写 design.md 其余内容。
      告知用户修改提案后再继续。
    - |
      关卡（安全敏感变更）：若变更涉及认证鉴权、外部 API 调用、凭证处理或
      部署配置，须额外调用 /cso，并追加记录：

        ## 关卡：安全审查
        状态：通过 | 阻断
        审查者：gstack/cso
        问题清单：<逐条列出，或填「无」>

  tasks:
    - |
      关卡检查：读取 design.md 顶部的工程审查关卡，确认状态为「通过」或「仅警告」。
      若状态为「阻断」或关卡章节缺失，立即停止并输出：
      「阻断：设计关卡未通过，请先修改提案。」
    - 每个任务的实现工作量控制在 2–3 小时以内。
    - 每个任务须注明它实现的是哪个 spec 章节，并标注影响层（前端/后端/全栈）。
